# DAY 8 — DETERMINISTIC RISK ENGINE

## Objective

Implement a deterministic risk engine for AegisGuard.

The risk engine evaluates the security risk associated with an authorization request.

The output is a score from:

```text
0 → 100